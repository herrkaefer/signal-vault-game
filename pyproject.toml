[project]
name = "signal-vault"
version = "0.1.0"
description = "A terminal-based grid game with dynamic narration"
requires-python = ">=3.11"
dependencies = [
    "edge-tts",
    "python-dotenv",
    "openai",
]

